[server]
# When running behind a reverse proxy, set root_url accordingly.
# root_url = %(protocol)s://%(domain)s:%(http_port)s/
# For simple local use, we keep it default and set GF_SERVER_ROOT_URL via env.

[users]
allow_sign_up = false

[metrics]
enabled = true
